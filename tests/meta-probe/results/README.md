# meta-probe results

Run outputs land here as `run-<host>-<timestamp>.json` plus `latest.json`.

These files are **gitignored** (machine-local analytics). Copy or attach them
when comparing hosts (black vs slower machines).

Schema: see parent `README.md` and a sample run after first `probe_driver.py run`.
