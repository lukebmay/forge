{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Forge Window Overrides",
  "description": "Configuration for window-specific tiling behavior in Forge GNOME extension",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference"
    },
    "overrides": {
      "type": "array",
      "description": "List of window override rules",
      "items": {
        "type": "object",
        "required": ["wmClass", "mode"],
        "properties": {
          "wmClass": {
            "type": "string",
            "description": "Window class to match. Use 'xprop WM_CLASS' or GNOME Looking Glass to find. Exact match (case-sensitive); comma-separates a list of exact classes."
          },
          "wmTitle": {
            "type": "string",
            "description": "Window title to match (optional). Supports: substring matching, '!' prefix for negation, comma-separated values for multiple patterns."
          },
          "wmId": {
            "type": "integer",
            "description": "Specific window ID (runtime use only, added by keyboard shortcut - not for manual configuration)"
          },
          "mode": {
            "type": "string",
            "enum": ["float", "tile", "ignore"],
            "description": "float = not tiled but still tracked; tile = force into tiling; ignore = Forge never manages (no tree node, no decorations, no session layout)"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "required": ["overrides"],
  "additionalProperties": false
}
