# P4 — Port Mark 2 OpSet → `lib/opsets/`

**Status:** done
**Updated:** 2026-08-28
**Implementer:** Grok 4.6 (architecture reshape)
**Brake:** `cd prototypes/container-motion && npm test` → **ALL PASSED
(154 cases)**
`npx vitest run tests/unit/opsets/mark2-module.test.js` → **3 passed**
`npx vitest run tests/unit/world/forest-world.test.js` → **6 passed**
`npx vitest run tests/unit/presenter/pane-rect.test.js` → **2 passed**
`npx vitest run tests/unit/session/forest-session.test.js` → **6 passed**
`npx vitest run tests/unit/keybinds/mark2-table.test.js` → **9 passed**
**Lock:** [`layers.md`](./layers.md) · D080 · D082 · D083 · **D084**

## Goal

Mark 2 OpSet is a gi-free product module on `lib/tom` + RuleSet + world.
Proto re-exports. Glossary stays `src/opsets/mark2.md` (one file; do not
fork). Product Move **is** this OpSet’s Move — Forge `tree.move` is not
a twin, but **this slice does not edit Forge CommandHandler / `tree.js`.**

## Architecture (do not rediscover)

**Homes:** `lib/opsets/` (Move/Join/Launch + transact) ·
`lib/world/neighbors.js` (edge / neighbor / sibling-axis; tie-break
**string**).

| Piece | Lives |
| --- | --- |
| SurfaceOps Move/Join/Launch/Toggle*/Promote*/Remove | `lib/opsets/mark2.js` |
| `runOpAbstract` | `lib/opsets/transact.js` |
| Registry `getOpSet` / `MARK2_OPSET` | `lib/opsets/index.js` |
| Neighbor / edge / sibling-axis (read world + TOM) | `lib/world/neighbors.js`. **Tie-break is a string argument** |
| `transferLeafToMonitor` | `lib/opsets/transfer.js`; settle via mark2 RuleSet. Not a Host mutator |
| `nearLeafOnMonitor` | proto presenter/focus (`monitors.mjs`) |
| Glossary | `prototypes/container-motion/src/opsets/mark2.md` |
| Proto `src/opsets/*.mjs` | re-export shims |
| `motionLog` in OpSet | dropped (no proto `plog.mjs` from `lib/`) |
| Forge `command.js` / `tree.move` / presets | **untouched** (P6 / later import) |

Why string-in: world must not import session (D082). OpSet hydrates
`sessionOf` then passes `aspectTieBreak`. Proto `monitors.mjs` wraps
the same helpers for Host/UI call sites that still omit the string.

## Landed

1. **`lib/opsets/{package.json,index.js,mark2.js,transact.js,transfer.js}`**
   — Mark 2 source. `getOpSet` / `MARK2_OPSET` / `runOpAbstract`.
   `copySession` + `copyWorld` around clone/commit (no regression).
2. **`lib/world/neighbors.js`** — `monitorsSiblingAxis`,
   `orderedMonitors`, `neighborMonitor`, `isAtMonitorEdge`,
   `dirInParentAxis`. Tie-break string (default HSPLIT). Re-exported
   from `lib/world/index.js`.
3. **`transferLeafToMonitor`** — OpSet place helper. After place, Mark 2
   `mark2CleanupUnder` (RuleSet max-1). Not left in proto Host.
4. **Proto shims** — `src/opsets/{index,mark2,transact}.mjs` re-export
   from `lib/opsets/`. `mark2Move` is the same binding.
5. **Proto `monitors.mjs`** — re-exports/wraps world neighbor queries
   (hydrates session when the string is omitted); keeps
   `nearLeafOnMonitor`. No `transferLeafToMonitor` twin.
6. **No proto plog** — `motionLog` dropped from the OpSet. `lib/` does
   not import `prototypes/container-motion/src/`.
7. **Vitest** `tests/unit/opsets/mark2-module.test.js` (3). Proto
   **154** green. Move / Join / Launch trees unchanged.

**Cycle:** `lib/opsets` → tom, rulesets, presenter, world, session.
World does **not** import opsets, session, or presenter. RuleSet does
not import world/session/opsets. Kernel does not import opsets.

Forge `tree.js` / `window.js` / `command.js` / `tree-snapshot.js` /
`keybind-presets.js` untouched.

## Do not (still)

- Edit `lib/extension/tree.js` or `window.js` or `command.js`
- Apply the vim table to shipping `keybind-presets.js` (P6)
- Wire CommandHandler dispatch (P6)
- Change Mark 2 Move/Join/Launch
- Commit or push unless asked

## Remaining (not this slice)

- **P6** — CommandHandler / shipping vim kit
- **P5** — epochs
