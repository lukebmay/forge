# P6 — Surface import: CommandHandler + shipping vim kit

**Status:** P6a done
**Updated:** 2026-08-28
**Lock:** [`keybinds.md`](./keybinds.md) · D080 · D081 · D084 · D085

## Goal

Surfaces dispatch **shared action ids** into Mark 2 OpSet. Shipping vim
kit chords **are** the Mark 2 table (Join, not swap). Do not grow a
second chord table.

## P6a landed

1. CommandHandler accepts dotted ids (`focus.*` / `move.*` / `join.*` /
   `focus.parent` / `focus.child`) plus aliases (`Focus`/`Move`/`Swap` +
   direction, `FocusParent`/`FocusChild`). `move.*` / `join.*` project
   the live GObject tree to TOM (skip FLOAT/GRAB_TILE/minimized), run
   Mark 2 OpSet, apply-back via D023 (hoist extras when dropping a CON),
   one `commitLayout`. Focus dirs still Host queries.
2. Shipping vim kit chords for those ids derived from `MARK2_TABLE`.
   `window-swap-*` gsettings keys stay as the Join surface. Safe/i3
   chord maps unchanged; they now dispatch Join on `window-swap-*`.
3. Product Move **is** Mark 2 Move. `tree.move` remains for other
   tests/regressions; CommandHandler does not call it.

## Files

- `lib/extension/tom-live.js` (new) — project + apply-back
- `lib/extension/command.js` — dotted ids; move/join via OpSet
- `lib/extension/keybindings.js` — KeybindAdapterGnome dispatch
- `lib/shared/keybind-presets.js` — vim overlay from `MARK2_TABLE`
- `lib/extension/cheatsheet.js` — Join category for directional swap keys
- `schemas/org.gnome.shell.extensions.forge.gschema.xml` — Join summaries
- `docs/user/keybindings.md` · `docs/user/layouts.md` · `agents/design.md`
- tests: `tests/unit/extension/tom-live.test.js` + updates listed below

## Brake

```
cd prototypes/container-motion && npm test
# ALL PASSED (154)

npx vitest run tests/unit/keybinds/mark2-table.test.js \
  tests/unit/shared/keybind-presets.test.js \
  tests/unit/command/CommandHandler.test.js \
  tests/unit/keybindings/Keybindings.test.js \
  tests/unit/window/WindowManager-commands.test.js \
  tests/unit/extension/structure-one-commit.test.js \
  tests/unit/opsets/mark2-module.test.js \
  tests/unit/extension/tom-live.test.js \
  tests/regression/bug-324-move-callback-stale-node.test.js
```

Counts: proto **154**; mark2-table **10**; keybind-presets **38**;
CommandHandler **82**; Keybindings **58**; WindowManager-commands **41**;
structure-one-commit **6**; opsets **3**; tom-live **4**; bug-324 **2**.
Kernel: opsets **3** + world **6** + presenter **2** + session **6** +
keybinds **10** + epochs **10**.

## Leftover (not P6a)

- Size / toggleSplit / promote still old CommandHandler bodies
- DnD execute not yet OpSet
- P5c Apply still GetTree
- `tree.move` still in `tree.js` (direct callers)
- First Move/Join on an n-child MONITOR wraps max-1 (Mark 2)
- Live dconf vim users keep Super+a until `forge keybind load vim`
- **D087:** FLOAT windows belong in FLOATS, not TILES. P6a skip-untiled
  is the stopgap — do not put floats under MONITOR
- **D088:** size/toggle/promote stay kernel ids; host overlays (quit,
  proto `a`/`q`) stay off the Mark 2 table
