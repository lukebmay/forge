# P6 — Surface import: CommandHandler + shipping vim kit

**Status:** ready
**Updated:** 2026-08-28
**Lock:** [`keybinds.md`](./keybinds.md) · D080 · D081 · D084 · D085

## Goal

Surfaces dispatch **shared action ids** into Mark 2 OpSet. Shipping vim
kit chords **are** the Mark 2 table (Join, not swap). Do not grow a
second chord table.

## First bite (P6a) — do not start until P6 is the PRIORITY row

1. CommandHandler (and proto `runAction` already) speak `lib/keybinds`
   action ids (`move.left`, `join.left`, …). Bodies call `lib/opsets`
   + `commitLayout`. Do **not** rewrite all of `command.js` in place;
   map the vim-kit ids first.
2. Apply Mark 2 table to shipping `keybind-presets.js` vim kit:
   `Ctrl+Super+hjkl` = Join (not swap); `p`/`Shift+p` parent/child.
   Safe/i3 stay host overlays on the **same ids**.
3. Product Move **is** Mark 2 Move (`lib/opsets`). `tree.move` is not a
   twin.

## Do not

- Merge the two monitor-resolves
- Retarget Apply onto T6 (P5c parked)
- Put Mutter in the kernel
- Invent overlay chords that duplicate Mark 2 ids
- Commit or push unless asked

## Context

- Table: `lib/keybinds/mark2.js` (Super-bearing)
- Proto: `stripSuper ∪ proto-overlay` (already generated)
- OpSet: `lib/opsets/`
- Shipping presets still historical swap / Super+a parent (P1c did not
  touch them)
- Glossary: `prototypes/container-motion/src/opsets/mark2.md`

## Brake

`cd prototypes/container-motion && npm test` → 154+
`npx vitest run tests/unit/keybinds/mark2-table.test.js`
plus CommandHandler / keybind-presets tests that exist today.
