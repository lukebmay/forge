# Design decisions

**How to use:** scan by Topic / Imp. Update in place when a decision changes;
set Status=`superseded` and add a new row (do not rewrite history silently).

| ID | Date | Topic | Imp | Status | Decision | Why |
| --- | --- | --- | --- | --- | --- | --- |
| D089 | 2026-08-28 | tiling | P0 | active | **Spread** is the leftover-percent word (`userSized === false` on a TILES child). Opposite is **sized**. Do not say float/share-floater for this. **FLOAT window** stays unmanaged (FLOATS bag, D087). *Share* remains the percent number. Action ids stay `size.float*` until a rename slice; labels/docs say Spread. | FLOAT windows and leftover percent were the same English word; share already means percent |
| D088 | 2026-08-28 | keybinds | P0 | active | **Keybind core is generic; each KeybindAdapter unions a host overlay.** Kernel = action ids + Super-bearing Mark 2 table. Overlays are adapter-local and are not imported by the other host. **WebView:** `Super+a` launch toy, `Super+q` OpSet remove (proto `stripSuper` → `a`/`q`). **Gnome:** `Super+q` = quit/close focused app; lock/zoom/run/prefs stay Gnome overlay. Safe/i3 = Gnome overlays on the **same kernel ids**. Same chord may map to different overlay ids per adapter. | Kernel must not own proto sandbox or GNOME quit; Super+q cannot be one id in both hosts |
| D087 | 2026-08-28 | architecture | P0 | active | **Forest document = META + FLOATS + TILES.** TILES is today's ROOT→WS→MONITOR tiling spine (WebView ROOT). FLOATS holds unmanaged FLOAT windows — **not** under a MONITOR, because a float can span heads. META is document facts only (not session prefs, not workarea). Mark 2 mutates TILES only. **Share floater** (`userSized === false` on a TILES child) ≠ **FLOAT window**. P6a skip-untiled is the stopgap until FLOATS ships. Re-tile = place into TILES. | Float-as-tiling-child cannot represent cross-mon floats; adapters need a kernel bag, not a Host mode bit |
| D086 | 2026-08-28 | architecture | P0 | active | **T6/epoch snapshot WINDOW key is `windowId` (string).** Algorithm lives in `lib/epochs/` (gi-free). Gnome adapter `tree-snapshot.js` may attach live Meta on the in-process document (`window` / `lastTabFocus`) for session-layout; epochs never read those. `resolveTargetMonitor` (majority/stableKey) is epochs; `resolveStrictMonitor` stays session-only. Live Forge `Node.nodeValue` remains Meta until a later surface import. | Three forest JSON shapes (Meta leaf / portable id / GetTree) blocked one TOM snapshot; Meta in the pure module mixed Host with the document |
| D085 | 2026-08-28 | architecture | P0 | active | **Kernel vs adapters.** Kernel = TOM + atomics + RuleSet + OpSet + keybind action-id table (JS `lib/` is the reference impl; in principle rewriteable in another language). Host adapters: **ForgeAdapterGnome** (today GJS `WindowManager` façade) and **ForgeAdapterWebView** (proto HTML desk). Keybind adapters: **KeybindAdapterGnome** (Super-bearing accels) and **KeybindAdapterWebView** (`stripSuper` ∪ proto overlay) on the **same** ids. World workarea is filled by the host adapter. Do not put Mutter/DOM/GNOME accels in the kernel. Code rename of WM → ForgeAdapterGnome is later; role is locked. | D079 said "same TOM, two presenters"; operators need named adapters so Forge is a WM for any system that gets an adapter |
| D084 | 2026-08-28 | architecture | P0 | active | **Mark 2 OpSet lives at `lib/opsets/`.** Neighbor / edge / sibling-axis queries live in `lib/world/neighbors.js` (`aspectTieBreak` is a **string argument**, like D082). `transferLeafToMonitor` is OpSet place + mark2 RuleSet max-1, not a Host mutator. Proto `src/opsets/*.mjs` re-export; proto `monitors.mjs` wraps neighbor queries and keeps `nearLeafOnMonitor`. No proto `plog.mjs` in `lib/`. Forge CommandHandler / `tree.move` / presets stay P6. | Proto World/plog imports made the OpSet unshareable |
| D083 | 2026-08-28 | architecture | P0 | active | **Forest/Node have no workarea.** MONITOR `{x,y,width,height,primary?}` lives in `lib/world/` (WeakMap keyed by Forest). Slot AABB `paneRect` / `wrapWouldViolateMin` live in `lib/presenter/`. `cloneForest` does not copy world; transact `copyWorld`s live↔draft. `createForest(geoms)` still takes geoms for monitor **count + ids**; TomApi/shorthand/Host `attachWorld`. Old proto dumps peel leftover `node.geom` on first `worldOf`. Forge `Node.rect` / `_createDecoration` stay leaks until import. | Paint/workarea on the document mixed Host with topology (same class of bug as D082 prefs) |
| D082 | 2026-08-27 | architecture | P0 | active | **Forest is TOM-only.** `decisions` / `mergeTags` / `peelModel` live in `lib/session/` (WeakMap keyed by Forest). `cloneForest` does not copy session; OpSet transact `copySession`s live↔draft. RuleSet coerce / `preferredSplitVsParent` take an `aspectTieBreak` **string** (OpSet hydrates then passes). Old proto dumps peel leftover fields on first `sessionOf`. | Policy on the document mixed prefs with topology; JSON clone smuggled OpSet prefs |
| D081 | 2026-08-27 | keybinds | P0 | active | **Vim / Mark 2 kit = proto right-hand main-reach.** Table: `hjkl` focus, `Shift+hjkl` Move, `Ctrl+hjkl` Join, `p`/`Shift+p` parent/child, `m`/`n` toggles, `[`/`]` cycle layout, `{`/`}` promote, `Alt+hjkl`/`yuio`/`nm,.`/`/`/`7890` size. Proto overlay stays `a`/`q`/Delete/tags/`f`. Not kit: leftover `yuio` extra-focus, TreeOp swap, old Forge vim Super+a parent / Ctrl+Super swap. Shipping `keybind-presets.js` apply still P4/P6 (CommandHandler must speak OpSet). | Proto is the condensed app; historical vim kit was Forge-implementation sand |
| D080 | 2026-08-27 | architecture | P0 | active | **Core** = TOM + atomics + composed (no settle). **RuleSet** = real gi-free module OpSets bind/extend/replace (`core` prune+unary+shares; `mark2` + coerce TABBED + MONITOR max-1). Unary after breakout is RuleSet, not a second Promote. Product **Move = Mark 2 Move**. **Keybind core** = one Super-bearing Mark 2 table; proto `stripSuper` + `a`/`q` overlay; Safe/i3 = Forge overlays on the same action ids. Join chord: Mark 2 table wins over Forge vim swap. | Proto must catch Forge bugs; docs-only settle drifted (`cleanTree` ≠ unary); two chord tables ruin the prototype |
| D079 | 2026-08-27 | architecture | P0 | active | **Option 2:** product kernel = proto TOM lifted to `lib/tom/` (gi-free, presenter-free). Forge `Node`/`Tree` discarded as kernel. WM reshaped to thin Host. Import apply/H1/session/CLI/chrome as layers. Same TOM in proto + Forge. Plan scan rebuilt PRIORITY. **Places D073/D074 as product kernel, not proto-only.** | Incremental extracts never split model from Meta/paint; duck-tape stacked on the wrong object |
| D078 | 2026-08-27 | containers | P1 | active | H/V child-list change: leftover → floaters; **no floaters left → rescale sized shares to 100%** (keep ratios). Node that **leaves** a CON becomes a floater (sized shares do not follow). Unary collapse still copies the CON’s slot. Dock launch = `Launch(MonN)` on that head; guake/`a` = `Launch()` on the selection’s monitor. Proto: per-monitor dock buttons; `q` = OpSet Remove | Close/move of last floater left a flex gap; dock vs selected launch were one menu |
| D077 | 2026-08-27 | containers | P1 | active | Mark 2 Launch selected = WINDOW **or CON** on that monitor (`p` counts); treat CON as the slot (same aspect wrap/sibling rules). Same-type wrap of an H/V CON: MONITOR → opposite split; nested → last child of that CON. TAB/STACK size ops (nudge/preset/float/equalize) target the bag slot, not leaf percents. `Alt+/` floats every node (equalize each H/V). **Supersedes D076 Launch leaf-only.** Float chords in D076 stay. | Operator: select tab group then launch nautilus under it (bottom-left); tab child size is irrelevant |
| D076 | 2026-08-27 | containers | P1 | active | Mark 2 Launch: selected = WINDOW leaf on that monitor; else append at end of that tree. TAB/STACK → next sibling. HSPLIT: wider-than-tall → sibling, else wrap VSPLIT. VSPLIT: taller-than-wide → sibling, else wrap HSPLIT. MONITOR sole child wraps in place (max-1). H/V size floor 10% → wrap TAB instead. Float chords: parent = cross-axis ancestor. `Alt+yuio` this / this+sibs / sibs / +parent; `Alt+nm,.` parent / parent group / parent sibs / both groups. `Alt+n`/`m` no longer mean float this / siblings. **Launch leaf-only superseded by D077.** | Operator: launch next to leaf by aspect; float parent is cross-axis share |
| D075 | 2026-08-27 | containers | P1 | active | H/V in-axis share on children (`percent`); cross-axis = parent container’s share. Float = not userSized; leftover equal among floaters. Floor 10%, step 5%. Proto resize `Alt+hjkl` / presets `7890` (no Super). **Float keys superseded by D076.** Tree graph is ROOT→WS→MONITOR (no fake “forest” node) | Operator: size is in-axis; fake forest node hid the spine |
| D074 | 2026-08-27 | containers | P0 | active | Mark 2 design doc is FIRM source of truth (`src/opsets/mark2.md`). Breakout = Promote (node becomes sibling of parent). Unary collapse = 1-child CON deleted, child takes its place. TOM spine ROOT→WS→MONITOR (mon 0/1 child). Join enters a CON at the **near** edge. OpSet doc change ⇒ same-effort code + tests. Join `H(TAB(A,B),TAB(C,D))` C← → `H(TAB(A,B,C),D)` | Vague “slot/host/promote” language was drifting the proto; Join into adjacent tab threw (looked like no-op) |
| D073 | 2026-08-27 | containers | P0 | active | Tiling Object Model (TOM): atomics + composed TreeOps + OpSets (Mark 2 first). Presenters consume settled TOM. “Molecule” retired. Proto kernel `prototypes/container-motion/src/tom/` is dual-use (no Mutter/DOM). Shorthand Given/Actions/Expect kept as lingua franca, not a product DSL. Tests: atomics, composed, OpSet surface, workflow capability suite | Policy-in-kernel caused the Mark 2 regression loop; need a clean surface for multiple control packs and a later Forge port |
| D071 | 2026-08-26 | containers | P1 | active | Mark 2 proto: same-type H/V repair → **TABBED** (not H↔V). Join invent opposite vs parent; if opposite equals a CON child → TAB. 2-window siblings: any join dir wraps pair. Abstract settle unchanged | H↔V coerce on unary promote undid 2-leaf V→H invent under H grandparent |
| D070 | 2026-08-26 | containers | P1 | active | Motion lineage named Mark 0 (OG Move), Mark 1 (luke C4 + tame-edge lean), Mark 2 (proto policy pack). Mark 2 prototype-only until adopt/abandon; newest meeting wins for proto | Need clear names while experimenting without implying Shell ship |
| D001 | 2026-08-06 | theming | P0 | active | Bundled CSS base + user overrides; never full-file patchCss clobber | Upgrades wiped personal colors; user file is deltas not a fork |
| D002 | 2026-08-07 | meta-probe | P1 | active | Intervalic agreement only; soft disagreements recorded, never reset settle timer | Title/focus noise must not block layout settle; keep soft timelines for later chrome/focus work |
| D003 | 2026-08-07 | meta-probe | P1 | superseded | Settle = hard-stable duration; cal@50ms bootstrap 10s; 1 cal+N full/op; write once at end | Superseded by D004 (per-app write + thrash campaign) |
| D004 | 2026-08-07 | meta-probe | P0 | active | Core apps 5×; sticky trials; per-app write; sleep inhibit; thrash delay sweeps | Sleep/crash lost matrices; open_warm piled windows; need thrash-free inter-op D for engine rewrite |
| D005 | 2026-08-07 | meta-probe | P0 | active | Meta baseline: multi-op thrash-free at D=0 (Forge off) on black | Core apps green; product thrash is Forge-induced, not Meta floor |
| D006 | 2026-08-07 | layout | P0 | active | Thrash/fail-open → float client + placeholder TILE leaf; never forest reassert | One bad app must not unsettle forest; close PH = drop slot + one reflow |
| D007 | 2026-08-08 | open-app | P1 | active | Dock sticky mon = pointer geometry; hook activate/open_new_window/activate_full; never rehome dock by focus | get_current_monitor/focus stole left-dock opens to right mon |
| D008 | 2026-08-08 | layout | P0 | active | Cold layout: skeleton (slot-tagged PHs) before bind; no Mode B mid-batch. Still the **materialize-forest** rule under D040 | Residual Mode B second pass is construction-order patch, not product fix |
| D009 | 2026-08-08 | layout | P0 | active | CT1: ensure_skeleton+bind ops; thrash park suppressed on cold/just_opened. One command; Mode B is not cold success | One CLI invocation; postOpenRetry opt-in only |
| D010 | 2026-08-08 | layout | P1 | superseded | Apply chrome from apply start through focus/soft | **Superseded by D043** — overlay dies at all-hard, not after soft |
| D011 | 2026-08-08 | layout | P0 | superseded | Belt re-focus + preserve lastTabFocus on ensure | Superseded by D014 (belt no structure; keep preserve as generic) |
| D012 | 2026-08-08 | layout | P0 | superseded | Final focus after settle + reassert | Superseded by D015 (one post-settle focus; reassert opt-in) |
| D013 | 2026-08-08 | open-app | P0 | active | Empty LFT(m) → last tile on mon; single dock pending wins appId drift | mon-root 3rd HSPLIT covered tabs; dock miss fell to focus mon |
| D014 | 2026-08-08 | layout | P0 | superseded | Post-open belt = pin-role wrong-mon moves only; no ensure_layout/order | **Superseded by D042** — open into slot; belt is a crutch to **delete** (SM6), not a product contract. Do not add belt features |
| D015 | 2026-08-08 | layout | P0 | superseded | One post-settle focus; no reassert | Superseded by D017 — quiet alone does not own late chrome steal |
| D016 | 2026-08-08 | layout | P0 | active | `_layoutOp` still preserves valid lastTabFocus on TABBED/STACKED re-affirm | Mid-session ensure_layout still anchors first id; generic safety not desk-specific |
| D017 | 2026-08-08 | layout | P0 | superseded | Final focus + verify-once only | Superseded by D018 — verify alone lost to multi-second chrome activate |
| D018 | 2026-08-08 | layout | P0 | active | Pin tab open leaf on focus; restore on meta-focus steal; explicit reveal of another child adopts the pin (R026); tab-active=lastTabFocus. Apply path does **not** poll GetTree (D037) | Cold: Chrome over Grok; tab chrome follows keyboard not open leaf; tab click during pin was treated as steal |
| D019 | 2026-08-08 | layout | P0 | active | Hard vs soft **split** + soft heuristics file + pin-on-steal remain. **Execution** (one-shot TILE-anywhere wait → continue; focus-shaped `ok`) **superseded by D040/D041** | Fixed ms reassert brittle; Meta has no settle ACK. Timeout-continue hid empty-mon fails |
| D020 | 2026-08-09 | windows | mid | active | `mode: "ignore"` in windows.json: no tree node / decorations / session claim; user config only (no bundled brands) | Float still tracks + processFloats; some apps need true hands-off |
| D021 | 2026-08-09 | CLI | P0 | active | Mutating forge commands run as durable one-shot jobs by default (attach); `--detach` no-wait only; no flag required for TTY survival; single-flight mutators; not a daemon | Closing agent TTY mid-layout aborted apply; session-bound workers inherit DBus/DISPLAY |
| D022 | 2026-08-10 | nest-test | P0 | active | Nest = value-first retest harness (avoid Wayland logout loops); separate logical host data (`FORGE_HOST` + nest config root); no UNIX test user v1; auto stop/cleanup; nest only when extension reload needed; host for no-code smokes; nest default 1 mon — multi-mon only when testing multi-mon behavior | Shared HOME heuristics/config taint parent; dual-mon dummy nest unblocks structure loop only when needed; test-user setup cost > isolation gain until data-root fails |
| D023 | 2026-08-12 | tree | P0 | active | Child list via Node (`appendChild`/`insertBefore`/`removeChild`/`replaceChildren`); no `childNodes`/`parentNode` assigns outside Node | R018 install HSPLIT swap was a hand-rolled splice; catalog Canonical APIs |
| D024 | 2026-08-13 | dnd | P0 | active | Drop no-op iff parent+order+layout already match; CENTER on H/V siblings is a group op via `mergeWindowsIntoGroup` | `_isNoOpDrop` treated CENTER as “already after sibling” so Grok→Chrome did nothing |
| D025 | 2026-08-13 | focus | P0 | active | Live show-in-group via one WM helper (`revealGroupChild`); snapshot may write `lastTabFocus` as data only; reveal reasserts that child to slot (R025); adopt live pin if revealing another child (R026) | Agents re-coded LTF+raise; tab strip / pin / activate drifted; hidden tab stayed FLOAT/old-slot; first tab click after layout snapped back |
| D026 | 2026-08-13 | geom | P0 | active | TILE slot is authority; unsolicited size/max/Meta-fs (no grab, no forge echo) restores to slot. **Idle-only** — not during ApplyEpoch or grab (D039). Lone Meta-max exemption only when `window-maximize-on-single` is on (default off). Zoom later is a presentation flag | VLC/end-of-video fs skipped by `tree.apply`; mid-apply restore fights slot machines; sole-tile max looked untiled (no purple border) when setting off |
| D027 | 2026-08-13 | open-app | P0 | active | Empty dest head (pointer mon, else window mon) beats LFT/focus; dock still wins; generic tiled-head opens stay OP1 LFT | Dock-miss + left LFT yanked right-head opens onto left end-of-tree (R021) |
| D028 | 2026-08-13 | dnd | P0 | active | Empty-mon user drop moves the dragged **leaf** only; workspace-change may still migrate a CON | `_rehomeWindowPreservingContainer` walked the VSPLIT when sibling Meta mon matched dest (R022) |
| D029 | 2026-08-13 | dnd | P0 | active | Orientation-mismatch edge drop wraps the target; never reuse a MONITOR that already has siblings | `numWin === 2` MONITOR reuse flattened BOTTOM into a 3-wide HSPLIT (R023) |
| D030 | 2026-08-13 | zoom | P1 | active | Presentation `zoomMode` on WINDOW (`null`/`full`/`horizontal`/`vertical`); `tree.apply` paints `zoomRect`; any chord clears any mode; not Meta max/fs/monocle | IC3 would undo Meta maximize; zoom is a paint flag (I4 slots stay) |
| D031 | 2026-08-13 | keybinds | P1 | active | Install does not rewrite kits unless `--kit=vim\|safe\|i3`; after install warn when live matches no kit; `--kit` loads that kit. CLI: `keybind save\|load` (no --reset/--profile); `vim`/`safe`/`i3` reserved profile names | Kit chords change in tree; dconf is sticky; Super+Enter stayed swap-last after zoom remap |
| D032 | 2026-08-13 | insert | P0 | active | New tiled window / same-axis edge drop **slot-splits the focused (or drop-target) unit** when that unit’s H/V parent already has siblings. Never a 3rd even H/V sibling. A leftover 1-child H/V CON **is** the slot (join it; retarget aspect). Orientation from the **slot rect**, not a stale Meta frame. 2nd on empty mon stays a MONITOR sibling. Drag: reorder in strip/row; CENTER joins; empty-mon leaf-only (R022); peel wrap-in-slot | Operator: 3 Nautilus became even HSPLIT; leftover wrap attached to MONITOR; batch skip. Even 3-way only after resize or `window-reset-sizes`. |
| D033 | 2026-08-13 | keybinds | P2 | active | Empty `launch-app-command` opens GNOME Run a Command (`Main.openRunDialog`); non-empty still spawns the command. Vim/i3 Run is Super+Space. `toggle-maximized` is cleared while Forge is enabled so Super+Return is Forge zoom; host-defaults must not bind it | Operator wanted Super+Enter maximize (GNOME-like) and Super+Space launch; leftover host-defaults Super+Return maximize stole zoom |
| D034 | 2026-08-13 | layout | P0 | active | Map-pin: title while waiting, then class-only leftover assign. Replan class-only claim. **Serialize chrome-family opens.** Late title re-renders like late wm-class. Product apply waits on **Meta signals**, not CLI GetTree poll | Green: parallel Chrome+Grok crashed Chrome; Grok mapped as New Tab so wait missed it |
| D035 | 2026-08-13 | layout | P0 | active | Layout pin/plan use tree windows **plus** a Meta census. `trackWindow` falls back to a live monitor node instead of dropping. `admitUntrackedWindows` before plan and during map-wait. Timeout dumps `seen` + `includeMeta` | R029 class-only pin cannot see X11 windows that never entered the tree; second apply launched again |
| D036 | 2026-08-14 | CLI | P0 | active | Control CLI language is Node. Product policy lives in `lib/shared/` as **gi-free** ESM. Prefs/extension/CLI are facades (GJS vs Node I/O). PATH entry is `cli/forge.mjs` (CN13); leftover Python via spawn. Zsh install stays in `scripts/`. **Do not** port `layout_plan`/`layout_apply` into `cli/` | Dual Python/JS drifted (keybind); Node is the product language; a cli/ planner port would be thrown away by D037 |
| D037 | 2026-08-14 | layout | P0 | active | Layout rearchitecture is in-process `ApplyLayout` (extension owns the run; wait on Meta signals). DBus/job/planner shape is D038. **Executor shape is D040** (slot machines), not the AL7 phase spine. IC4 CLI-waiter fold is **skipped** | CLI GetTree polls are the slowness; language port of the poll loop is not |
| D038 | 2026-08-15 | layout | P0 | active | `ApplyLayout` is async start (not a blocking DBus call) plus `GetLayoutApply` / `CancelLayoutApply` and Progress/Done signals. Host D021 job is observer only. `planReconcile` lives in `lib/shared/layout-plan.js` (GetTree JSON in / actions out). LayoutBatch stays a primitive. R027 chrome remains in-progress; lifetime = apply epoch through all-hard (D043). Executor never `_layoutOp` flatten | GNOME DBus timeout cannot hold the spine; TTY survival stays host-side; planner must be gi-free; REG-ensure-flatten is not the cold path |
| D039 | 2026-08-16 | layout | P0 | active | ApplyEpoch / home authority: while ApplyLayout is live, desired forest is the only writer of mon membership and TILE home. `beginApplyEpoch` / `endApplyEpoch` replace ad-hoc `_layoutApplyLive`. Drop deferred entered-monitor rehomes on enter and leave. User grab and workareas are different epochs. Workareas/monitors-changed during apply → cancel (`displays-changed`). D026 TILE restore is idle-only (not during apply epoch or grab) | R036: entered-monitor rehome after open undid mon place; boolean suppress on WindowManager is a gate, not an API |
| D040 | 2026-08-16 | layout | P0 | active | Slot machines amend D019 execution. A **slot** is a desired-forest leaf (one TILE window **or** one TABBED/STACKED CON). Parallel across independent slots; serial inside a slot. Hard = in-slot (TILE\|grab + desired mon + desired parent CON + ε rect). Timeout retries that slot’s place (N=2 extra; first wait 5s, retry 2s). Late Meta resume only while the epoch is live. Soft + focus after all required slots terminal. One-shot hard continue is dead | TILE-anywhere hard + continue-on-timeout let `layout` report ok with mon1 empty; windows are not independent |
| D041 | 2026-08-16 | layout | P0 | active | `Done.ok` = forest match for every **required** TILE slot. Focus verify is not success. Required `hard-failed` → `ok: false`, `code: hard-failed`, slot list. Peers always finish (execution continues). FLOAT / `ignore` / non-tile are not required hard targets. Frequent hard-fails mean the hard contract is wrong — do not flip default `ok` true. No standing best-effort success | False-ok (R035/R036) hid structure bugs; hard-fail should be rare once hard means in-slot |
| D042 | 2026-08-16 | layout | P0 | active | Apply open binds **into the slot** (skeleton PH or slot id). PlaceNext dest is never mon-root-only on the product apply path. Belt + `beltStructure` are **not** product architecture (delete in SM6). **Supersedes D014** | Four-pass place fought Meta and lied about success |
| D043 | 2026-08-16 | chrome | P0 | superseded | Apply overlay through hard+soft (clear when soft skipped/done); settle notices on modal | **Superseded by D071** — clear at Done after forest-match; soft-end clear left desk clickable while thrash/rect still wrong |
| D070 | 2026-08-26 | layout | P0 | active | After primary forest-match **fail**: **production** runs **one** structure failsafe (`ensure_layout`/`ensure_order` for failed slots → rematch). Log WARN/ERROR; `result.failsafe`. Rematch ok → Done.ok with `failsafe.recovered`. **Dev (`production=false`)** / `flags.forestFailsafe:false`: **no** auto-recover — loud hard-failed so primary path (R042) is fixed. Opt-in nest: `FORGE_LAYOUT_FAILSAFE=1`. Never Mode B / belt. Failsafe is **not** the architecture | R042 thrash: CLI exit 1 but desk left wrong; prod needs guardrail; dev must not paper over |
| D047 | 2026-08-19 | chrome | P1 | active | Remove H/V split chrome / split-direction one-edge borders (`.window-split-border`, `split-border-toggle`, `split-chrome-show-all`, grab force). Keep focus borders + drag/place preview | Blue partial edges confused with place-next; ancestry paint on siblings was noise; operator chose delete |
| D048 | 2026-08-19 | install | P0 | active | Wayland `./install` never disable/enable a live Forge and never ends the session. Files overlay only; tip via nest or later logout. X11 may disable→replace→HUP. `forge_enable_extension` must not force-disable first | Host disable→enable SIGTRAP'd gnome-shell (looked like logout) and killed agents |
| D044 | 2026-08-16 | tabs | P0 | active | TABBED/STACKED CON is **mon-local**. Cross-mon groups are not product. Mixed-mon members rehome to the CON’s MONITOR ancestor (keep the group; do not auto-peel). Join across mons = move-then-join onto dest. Keyboard mon-move of one tab peels that leaf. No profile span sugar. H1 majority-align + R016 mon-loss collect stay survival-only | Operator did not know groups could straddle; one strip+pane cannot paint two heads; slot machines assume one mon per slot |
| D045 | 2026-08-17 | CLI | P1 | active | User `forge` is product-only (layout/tree/ops/settings/install). Nest + live matrix live on **`forge-test`** (`./scripts/forge/forge-test`). Normal `./install` does not put `forge-test` on PATH; `--with-test-cli` is opt-in. `forge test` / `forge nested` hard-break (exit 2) | User CLI ≠ dev toolkit; nested-under-`forge test` still shipped harnesses to ordinary install |
| D046 | 2026-08-18 | tabs | P1 | active | Tab strip DnD = Chrome live: `#forge-tab-chrome` pickability; REORDER = float chip + gap + **pointer×center** scoot; commit on release via `applyTabStripReorder` + `replaceChildren`; peel = synthetic MOVE APP (`_startTabMoveGrab`); wrap via `planTabbedWrap` (`min-tab-label-chars` default **12**); every release `clearTabDragResiduals`. No outline-on-neighbor product UI; no second DnD engine; D044 mon-local unchanged | Outline insert marker lied about side; window_group restack buried strips; leading-edge scoot hid index 0; stuck pressed/zones after click |
| D049 | 2026-08-19 | mins | P0 | shipped | Default tile mins from env `FORGE_MIN_TILE_WIDTH`/`FORGE_MIN_TILE_HEIGHT` (unset → **256×144**). Passive learn only (clamp vs forge request / frame larger than slot → class `window-mins.json`). **No shrink-probe** (tiny discover-resize deleted). Overflow → BFS same-mon tab → float + remove vacated gap. DnD red zones use effective mins. ApplyEpoch/PlaceNext pins excluded. Tiny-pane QoL stays separate | Shrink-probe raced grabs/tiles; Wayland hints missing; probe forever-retry; open-min BFS already the right policy |
| D050 | 2026-08-22 | logging | P0 | active | Dual-sink plog (shellrc **1.3.0** / D064+D066): file(+jsonl) = at/above effective (`~/.local/state/forge/forge.log` + sibling `.jsonl`); journal **WARN/ERROR/fatal only** (not INFO). GJS uses Gio `plog.gjs.js`; Node CLI uses `plog.js`. Custom levels mirror prefs FATAL/ALL. `production=true` does **not** force logging OFF | INFO→journal was still noisy for eyes-on; prod must keep INFO/WARN/ERROR on file |
| D054 | 2026-08-23 | logging | P1 | active | Dual-tape + fields policy: forge **opts in** JSONL beside hunt file (D066; `FORGE_LOG_JSONL=0` disables). **WARN/ERROR/fatal:** no structured `{ fields }` — adapter flattens into the message so journal matches `.log`. **INFO/DEBUG/TRACE:** short title + optional `{ fields }` for searchable JSONL payload. `forge log query|show|…` / query flags forward to vendored `third_party/plog-query` (defaults `P_LOG_FILE`/`P_LOG_JSONL` to forge tapes). Level ops (D053) unchanged for positional LEVEL/reset/--persist/--truncate | Journal cannot show JSONL payload; structured detail belongs on file-only levels; shell-first query is the product surface |
| D067 | 2026-08-23 | logging | P1 | active | Query pretty (shellrc plog-query **1.1.0**): header `#sid:pid:seq`; TTY pretty body / bat optional / match hilight. Forge: inherit TTY for auto color (Q0); re-vendor query CLI (Q5); tile-slot-float hunts emit `{ fields }` so JSONL `payload` is real (Q6). D054 flatten policy unchanged | Piped spawn hid TTY color; flat hunt strings left pretty with empty payload |
| D051 | 2026-08-22 | float | P0 | active | Float gatherer normalizes Meta `allows_resize()` via `allowsResizeForFloatPolicy`: maximized/fullscreen → treat as resizable for float policy. Permanent `no-resize` only when not covering. | Meta reports false while max/fs; processFloats TILE→FLOAT beat D026; vinyl Inkscape late-adopt stayed FLOAT |
| D052 | 2026-08-22 | logging | P1 | superseded | Regular/`--prod` → INFO; `--dev` → DEBUG; TRACE opt-in. Superseded by **D068** | TRACE-as-dev-default made 40MB+ files before dual-tape/query discipline |
| D068 | 2026-08-23 | logging | P1 | active | Install levels: regular → **INFO (4)**; `--dev` → **TRACE (6)**; `--prod` → **WARN (3)** (+ logging-enabled). Keep **dual-sink** always (file+jsonl at/above effective; journal WARN+ only) — do not journal-only on prod. `forge log LEVEL` still raises hunts without tip reload. Truncate-on-enable unchanged. | Queryable dual-tape made TRACE-as-dev useful; prod should stay quiet; journal-only would drop hunt JSONL when level raised |
| D053 | 2026-08-22 | logging | P1 | active | `forge log` live Shell level: session override (default) + `--persist` gsettings + `--truncate`. Settings `changed::log-level`/`logging-enabled` → plog `reconfigure()` (no tip reload). DBus `Log` (apiVersion 11). No top-level `forge --log-level` | Live TRACE raise via `forge set` left plog min stale; hunts needed session bump without logout |
| D069 | 2026-08-24 | tabs | P0 | active | **FIRM** tab/stack geometry: (1) every TILE peer shares the CON content rect; size on **join** and whenever the **group slot** moves/resizes via commit (`render`/`apply` + `reassertAllTabStackSlots` + post-echo heal) — tab click is not the primary size path; (2) **visible-first**: open leaf (`lastTabFocus`) before buried peers — never queue open-leaf `move_resize` behind buried work; buried peers stay mapped and may heal same-turn after visible and/or idle/post-echo; (3) reveal = raise/focus + R025 safety net on the revealed child only — no all-peer reassert on focus; (4) **no drift** without a very significant reason — present trade-offs and get explicit go first. Plan: `agents/plans/forge-tab-peer-geometry.md`. Does not replace D025 (reveal API) or D026 (TILE slot authority). **2026-08-26:** epoch-end `reassertAllTabStackSlots({force:true})` (still no afterFocus reassert — PWA thrash) | Chrome half-height over full Grok until click; verify is log-only; focus-path all-peer reassert caused PWA thrash; operator locked shared-slot + visible-first |
| D071 | 2026-08-26 | chrome | P0 | active | Apply overlay clears only at **Done** (`_finish`: done / hard-failed / error / cancel) — after soft **and** verify/forest-match. Soft/hard barriers must not `_clearChrome` early. Epoch leave force-heals tab peers (D069). Supersedes D043 soft-end clear | Soft-end clear + thrash left desk clickable while mon1 3-col / Grok rect-mismatch; modal must outlast settle |
| D072 | 2026-08-26 | layout | P1 | active | Opt-in layout chaos (`FORGE_LAYOUT_CHAOS=1` / `flags.chaos`; seed `FORGE_LAYOUT_CHAOS_SEED`). Slice 1: random launch-order shuffle + inter-group delays (50–400ms), each independently on/off. Always log cocktail. Nest TODO queue: `agents/tasks/forge-layout-chaos-nest-queue.md`. Off unless env/flag — `./install --dev` alone must not chaos daily driver | Harden open races (R042 class) without ambient host thrash |
