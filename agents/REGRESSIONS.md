# Layout / open-app regressions

**Purpose:** stop re-fixing the same dual-mon bugs without a durable record.
When a live failure repeats a known class, **add a unit test that fails without
the fix**, then a row here. Plan session notes are not enough.

| ID | Date | Symptom | Root cause | Fix | Guard test(s) |
| --- | --- | --- | --- | --- | --- |
| R001 | 2026-08-06 | mon0 tab shows Chrome not Grok | belt `ensure_layout` anchors first id; stomps lastTabFocus | D016 preserve lastTabFocus + D014 belt no structure | `session-api-layout-cycle` layout TABBED re-affirm; `belt_actions` pin-moves-only |
| R002 | 2026-08-08 | same after cold open | mid-flight focus before chrome map; late activate steals | D017 focus + verify-once on lastTabFocus mismatch | `focus_actions_still_needed`; final focus verify in CLI |
| R005 | 2026-08-08 | cold X11: Chrome over Grok; wrong tab selected | first focus sticks then chrome/PWA late activate rewrites lastTabFocus; tab-active followed kbd not open leaf | D018 pin+restore; tab-active=lastTabFocus; SE5 pin 15s; SE3 soft barrier | `layout-open-leaf-pin`; `run_soft_focus_barrier`; `focus_actions_still_needed` |
| R006 | 2026-08-09 | fixed 250–2s reassert brittle across host speeds | soft residual not modeled; Meta has no settle ACK | D019 hard-ready 5s + soft barrier from settle-heuristics + post-settled verify once | `run_soft_focus_barrier`; `wait_until_hard_ready`; `test_settle_heuristics`; `TestTimeoutResilience` (soft undersize → verify/learn; hard timeout detect/continue; wall spin-safe) |
| R007 | 2026-08-09 | cold/mid: Chrome over Grok again; left ghostty not focused after layout | (1) active focus used Meta.activate → kbd thrash; (2) no-open path skipped soft barrier; (3) learned soft floor 400ms too short after opens; (4) RunSteps dropped `keyboard:false`; (5) save focus ignored floats / no LFT fallback | open-leaf focus `keyboard:false`; always final soft focus; cold soft floor 2s with pins; run-steps passthrough; save focus = last saved item (+ LFT); `--focus` CLI | `test_focus_active_open_leaf_no_keyboard`; `test_focus_from_float_*`; `test_parse_focus_cli_and_override` |
| R008 | 2026-08-09 | **true cold** mon0 Chrome New Tab over Grok (partial path green) | residual desk / pre-R007 path; **not** reproduced after R007 cold soft floor + pin | verified green SE8b (2× true cold Guake + partial) — R007 path holds | `L2.true-cold-dev` / R008 live; soft barrier + open-leaf pin |
| R009 | 2026-08-09 | `forge layout clean` / show fails on `{tiles:[]}` | `detect_layout_mode` required non-empty tiles | empty `tiles` list/dict + empty `roles:[]` count as reconcile sugar (CE1) | `test_empty_tiles_list_is_reconcile`; `test_empty_roles_list_is_reconcile` |
| R010 | 2026-08-09 | **Wayland** multi-open residual: mon1 tabs flat / wrong mon after first Mode A apply; second apply Mode B thrash-recover repaired | **Not** “Meta has no settled event” (that is **D019 soft residual** — intentional product fallback). R010 was **structure phase**: same-batch mon HSPLIT after TABBED flattened mon1; place+ensure without replan used pre-move mon paths | **Shipped:** mon-ensure skip when nested structure planned; residual place→structure replan. Soft residual + in-command verify = product (not a bug). Mode B as cold *success* still rejected. Re-check first-shot multi-open only as structure residual — do not treat soft-timeout as R010 | `partition_extension_steps_place_vs_structure`; mon-ensure skip in `layout_plan` |
| R011 | 2026-08-10 | **X11** partial chrome reopen (`L1.ghosttys-only` / `L1.right-ghostty`): mon0 open leaf stuck on Chrome (Walmart title); Grok flat at `mo0ws0/2`; soft focus max-corrections (32); dry-run `tabbed-roles-not-grouped:mon0.s0` | place→structure split treated **all** `move` as place. `ensure_layout` tabbed expands to `layout(anchor)` + `move(peer→id:anchor)`; join moves ran **before** TABBED wrap → solo TABBED(Chrome) + Grok mon sibling | **Shipped:** window-dest moves (`dest id:…`) stay in structure after layout wrap; mon-path moves remain place | `test_partition_tab_join_moves_stay_with_structure`; live `L1.ghosttys-only` / `L1.right-ghostty` |
| R012 | 2026-08-10 | **X11** DnD: tab-join Nautilus onto left mon Ghostty after right-mon tab group → mon **HSPLIT** instead of TABBED; TOP then center worked | Meta `window-entered-monitor` mid **GRAB_TILE** rehomed as mon LFT sibling (HSPLIT); target frame shrunk; pointer often over self → null drop → stuck HSPLIT | **Shipped:** skip rehome while GRAB_TILE / `_draggedNodeWindow`; grab-end re-resolves `nodeWinAtPointer`; RunSteps `dnd-drop` for live | L0 `bug-r012-grabtile-no-mid-drag-rehome`; live `L1.r012-cross-mon-tab-dnd` (`--tags R012` / `--from-work dnd`) |
| R013 | 2026-08-10 | **Wayland** multi-open residual (`L1.ghosttys-only` / sequence → `L1.right-ghostty`): mon1 tabs flat or mon0 TABBED polluted with ghostty; soft focus max-corrections (32); open leaf Ghostty | residual place→structure built mon1 TABBED, then Meta rehomed pin roles to mon0; **belt** mon-root moves fixed mon but **flattened** TABBED (no structure after belt). Soft barrier cannot repair structure | **Shipped:** after belt pin mon-moves, one place→structure rebind (`beltStructure` / optional `beltPlace2`) — same contract as residual follow-up; not Mode B | live `L1.ghosttys-only` / `L1.right-ghostty`; apply log `beltStructure` |
| R014 | 2026-08-10 | **Wayland** soft focus max-corrections (32); open leaf stuck on focused TILE sibling (e.g. Ghostty) after keyboard:false focus on Grok reports ok | **GetTree mutated live tree:** `syncLastTabFocusFromFocus` on every GetTree stomped `lastTabFocus` to Meta keyboard focus. Soft barrier set open leaf then polled GetTree which rewrote LTF to focused ghostty in same TABBED — infinite thrash. Violates D018 (open leaf ≠ kbd focus) | **Shipped:** remove GetTree-side sync; session save keeps its own sync path; **host RC green** after logout loads tip | live soft settle; keyboard:false focus + GetTree must preserve LTF; `wayland-rc` 2026-08-10 |
| R015 | 2026-08-10 | **Wayland** host: drag TILE from mon0 onto **empty** mon1 → snaps back on release; keyboard mon-move works | DnD only commits when `nodeWinAtPointer` is set; R012 skips mid-drag rehome while GRAB_TILE; empty dest mon → null target → grab-end no-op → `commitLayout` snaps geometry to source mon | **Shipped:** grab-end empty-mon path (`resolveEmptyMonitorDrop` + `_commitEmptyMonitorDrop`); session `dnd-drop` with `destMonitor`; live `L1.r015-empty-mon-dnd` | L0 `bug-r015-empty-mon-dnd`; live `--tags R015` / `--from-work dnd` |
| R016 | 2026-08-11 | **Wayland/X11** dual-mon: **no-op** monitor re-apply (e.g. `gdisplays load` when layout already correct, or Mutter re-`ApplyMonitorsConfig` with identical geometry) **thrashes** tiled desk — windows jump / tabs reshuffle / focus storm — instead of calm retile or true no-op | `workareas-changed` (and related) arms **H1 thrash-pending + monitor-recovery** even when mon geometry fingerprint is **unchanged**. H1 is correct for lock/wake GPU peel; wrong for false thrash. shellrc gdisplays **6.4.2** skips no-op apply at caller; Forge must still short-circuit | **Shipped:** `workareas-policy` fingerprint + classify; signal no-op when fp+homes match; settle graduated (noop / retile / mon_gain / mon_loss collect-to-end / H1 thrash); quiet fp on `snapshotLastGoodHomes` | L0 `workareas-policy` + `bug-r016-noop-workareas-no-thrash`; live `L1.r016-noop-workareas` (`--tags R016`) |
| R017 | 2026-08-11 | **Wayland** dual-mon: scale change thrash. **1.5→1.0** often OK after first fix; **1.0→1.5 reverse** still destroyed topology (mon0 ghostty → mon1) | (A) entered-monitor race + quiet poison; (B) settle classifies scale as **thrash→H1** because `geom:` stableKeys rewrite on every size change (lost+gained all heads); H1 mis-rehomes via **stale-scale frames** before last-good index | **Shipped:** entered-monitor suppress/defer/grace; **same mon count → retile** (geom change) / **renumber** (geom quiet); Meta→tree align on retile; H1 prefers last-good **index before frame**. Nest: both scale dirs `workareas-retile`. **Host tip:** logout after install | L0 `bug-r017-…` + `workareas-policy`; live note `--tags R017` |
| R018 | 2026-08-12 | **Wayland** `forge install` after `layout dev`: right mon HSPLIT swaps (term\|tab → tab\|term). Left `[tab\|term]` stays. Install is not a layout apply. | Session restore mixed-mon splice: extra sibling (desktop icon / float) → `insertBefore(rebuilt, anchor)` when `anchor === rebuilt[0]` is a no-op, then the tab CON lands *before* the term. Wayland `forge_restart_shell` then flushed that swapped tree (`immediate` bypasses 12s hold) even though there is no HUP. | **Shipped:** mixed splice = extrasBefore + rebuilt + extrasAfter (desc order). Flush session-layout only on X11 HUP. | L0 `tree-snapshot` mixed term\|tab / tab\|term extras |
| R019 | 2026-08-13 | Drag lower VSPLIT sibling onto upper CENTER (Grok onto Chrome) does nothing; reverse gesture tabs | `_isNoOpDrop` treated CENTER as “already after target” and ignored layout change | **Shipped:** `dropChangesStructure` (D024); CENTER groups via `mergeWindowsIntoGroup` | L0 `drop-intent` + comprehensive CENTER both directions |
| R020 | 2026-08-13 | Tiled app (e.g. VLC) Meta-fullscreens or resizes off-slot without a grab | `tree.apply` skipped fullscreen; `onExternalGeometry` log-only; max floated | **Shipped:** D026 `shouldRestoreTileSlot` + `_restoreTileToSlot`. **Live (2026-08-15 post-AL8):** nest mon=1 — tiled VLC + fixture play-to-EOS stayed TILE in slot; post-EOS Meta max+fs restored to same slot (no FLOAT off-slot). Nest video decode/vout weak (`vdummy`); natural VLC EOS-fullscreen UI may not fire there — contract path proven on that TILE. Host eyes-on optional after logout tip. No extra code. | L0 `bug-461-edge-snap` / `layout-sensors` restore; lone-max still `bug-dyt2`; live `--tags R020` |
| R021 | 2026-08-13 | Open nautilus on empty right mon attaches left tree end (bottom-right) | OP1 LFT/focus + `_lastTileOnMonitor`; no empty-head rule; dock miss | **Shipped:** D027 empty dest head (pointer then window mon) beats LFT; focus cannot steal | L0 `lft-mru` + `open-app-policy` + `bug-r021-r024-open-drop-layout`; live `L1.r021-empty-head-open` |
| R022 | 2026-08-13 | Drag nested VSPLIT leaf to empty mon moves the whole VSPLIT | `_commitEmptyMonitorDrop` reused `_rehomeWindowPreservingContainer` (workspace-migrate) | **Shipped:** D028 empty-mon DnD is leaf-only; attach after dest LFT or mon root | L0 `bug-r015-empty-mon-dnd` nested case + `bug-r021-r024`; live `L1.r022-nested-empty-mon-dnd` |
| R023 | 2026-08-13 | BOTTOM on left of 2-wide MONITOR HSPLIT → 3-wide HSPLIT | `shouldCreateCon` reused MONITOR when `numWin === 2` | **Shipped:** D029 wrap the target; never reuse a multi-child MONITOR | L0 comprehensive MONITOR BOTTOM + `bug-r021-r024`; live `L1.r023-bottom-nest-hsplit` |
| R024 | 2026-08-13 | First `forge layout` leaves windows at FLOAT geometry; second tiles (wrong sizes) | Mid-batch RunSteps `renderTree` **clears** the batch latch; `endOpenLayoutBatch` then skipped force-paint after releasing deferred FLOAT. Incomplete paint wrote min-size percents that the second apply kept | **Shipped:** batch end **always** force-paints after release; skip percent write-back while batch is active | L0 `bug-r021-r024` mid-batch latch; `open-commit` residual+end; `t4-sizing-policy` skipWriteBack |
| R025 | 2026-08-13 | Click tab reveals the app at FLOAT/old size | `revealGroupChild` raised without `reassertNodeToSlot`; hidden tab never moved; afterFocus must not reassert (PWA frame-lie) | **Shipped:** reveal reasserts that child only (skip zoom); still no afterFocus reassert | L0 `action-pipeline` R025 |
| R026 | 2026-08-13 | After `layout dev`, first click of the other tab (Chrome) flashes then Grok snaps back; second click sticks | Layout pin (D018, 15s) still held Grok; tab-click `revealGroupChild` did not adopt; Meta focus after `activate` looked like steal → restore | **Shipped:** reveal of a different child while a pin is live adopts that child as the pin | L0 `action-pipeline` R026; live `L1.r026-tab-click-adopts-pin` |
| R027 | 2026-08-13 | Loading overlay gone (or never shown) while apply still owns focus/soft; clicks go through | Chrome only on LayoutBatch begin (opens path); cleared after residual place; scrim `reactive: false` | **Shipped:** `chrome-show` at apply start (incl. no-open); clear after focus/soft; scrim eats pointer | L0 `session-api-layout-cycle` chrome-show; live `L1.r027-chrome-until-ready` |
| R028 | 2026-08-13 | After `layout dev`, dock Nautilus on left → 3-wide even HSPLIT (not VSPLIT of the focused slot) | (1) leftover 1-child H/V treated as a no-op then attached to MONITOR; (2) CL8 skipped wrap while layout batch was still active; (3) wrap orientation used a stale wide `get_frame_rect` instead of the slot; **(4) live 2026-08-14:** Nautilus maps `class=null title=null` → `isFloatingExempt` skips D032; `processFloats` tiles in place as MONITOR's 3rd sibling | **Shipped:** join leftover 1-child H/V as the slot (retarget aspect); D032 still runs mid-batch; orientation from slot rect; **(4) adopt into D032 wrap on FLOAT→TILE** (`_adoptOpenIntoTileSlot`; do not reserve a wrap at unknown map — R031) | L0 `WindowManager-insert-slot-split` leftover join + batch open + slot-rect + late null class/title |
| R029 | 2026-08-13 | Green `layout dev`: windows FLOAT, Chrome crash dialog, second apply **doubles** apps, still no TILE | (1) parallel `google-chrome` + Grok PWA same profile crashes Chrome on slow X11; (2) map-pin `title~=Grok` refused the PWA while it was still “New Tab”; apply aborted; next run launched again; (3) empty title at map → `isFloatingExempt` FLOAT, no `notify::title` re-render | **Shipped:** serialize chrome-family opens; class-only leftover pin at map-wait timeout; class-only replan claim; `notify::title` → renderTree (D034) | L0 `test_wait_class_fallback_*`; `TestClaimClassFallback`; `bug-r029-late-title` |
| R030 | 2026-08-13 | After R029, first green `layout dev` still no TILE; second apply **opens again**. X11 has Grok + Ghostty windows that GetTree never lists | Map-wait / plan only see tree windows. `trackWindow` dropped maps when dest `moNwsW` was missing (`reloadTree` idle, no retry). Live Meta windows stayed untracked → pin timeout → next apply launched duplicates | **Shipped:** dest fallback + `admitUntrackedWindows` before plan and during map-wait; GetTree `includeMeta`; timeout dump (D035) | L0 `bug-r030-untracked-map-pin`; `window-census`; `test_wait_pins_by_wm_class_instance` |
| R031 | 2026-08-15 | **Open float** (Kooha / always-float): window floats correctly, but tree gains a **blank TILE slot** with **red float border** stuck on that ghost slot; border does not follow the real float | (1) R028 wrapped at unknown map even when the window stayed FLOAT; willTile wrap then `is_above`/override left `[TILE, FLOAT]` as a reserved slot. (2) `showWindowBorders` / `paintRectForWindow` painted FLOAT from stale tree slot, not Meta frame (Wave Z TILE/ZOOM preference) | **Shipped:** D032 wrap only when `willTile`; late TILE via `_adoptOpenIntoTileSlot`; unwind FLOAT from a 2-child H/V wrap; FLOAT border + `paintRectForWindow` use Meta frame / clear slot rect | L0 `bug-r031-float-border-ghost-tile`; R028 late-identity still wraps on TILE; nest Kooha FLOAT no wrap; live `L1.r031-float-border-follows` (`--tags R031`) |
| R032 | 2026-08-15 | TABBED strip: clicks often do nothing (cannot focus tab items). Workarounds: click a non-group tile then back, or click the app body (not the tab) then strip works again | ApplyLayout dropped WR14 restack; last raise left deco under window actors (Chrome/stale frames often cover the strip). Trailing/`settleTabFocus` raise re-buried chrome | **Shipped:** WR14 settle on ApplyLayout steps; **Done restack-only** (no second raise); keyboard `revealGroupChild` focus+activate then `afterFocus` | L0 `bug-tab-click-activate` R032 + `action-pipeline` keyboard order; nest smoke PASS; live `L1.r032-tab-click-responsive` (`--tags R032`); host tip needs logout |
| R033 | 2026-08-15 | **Open / launch** (dock or `forge launch`): new app does **not** split LFT by unit aspect the way it used to. Taller-than-wide LFT unit should become **first child of a VSPLIT**, new app **second**; wider-than-tall → **HSPLIT** (same order). Wrong orientation or attach-as-sibling thrash | **Shipped (code):** open aspect used frame-first / `unit.rect` only — stale wide Meta frame forced HSPLIT; `_slotRectForUnit` now `paintRect/renderRect/rect/frame`. Bag attach used `!isWindow` so MONITOR PlaceNext attached under workspace then mon-root rehome. D032 wrap + child order unchanged when slot known | L0 insert-slot-split R033 tall/wide + renderRect-only; open-app-policy 1-child slot vs frame; 88 related green. Nest client map empty (not structure). Host logout tip then tall/wide LFT + launch |
| R034 | 2026-08-15 | `forge layout dev` / ApplyLayout open: **YouTube TV** PWA launches instead of **YouTube** (or similar Name-prefix PWA pairs) | `Gio.DesktopAppInfo.search("YouTube")` returns both `YouTube` and `YouTube TV` in one group; first-id pick is order-dependent. Multi-word path also increased Name-search use | **Shipped (code):** `pickDesktopSearchResult` prefers **exact Name** (casefold), then prefix; chrome `wm_class` desktop id still preferred first. Host tip needs logout | L0 `layout-open` YouTube vs YouTube TV pick; host after logout: layout/open YouTube role must map `chrome-agimnkij…` not `chrome-nlmaamao…` |
| R035 | 2026-08-15 | Cold `forge layout dev` exit 0 / verify match but **mon1 tab roles flat** (YouTube/Gmail/Voice as mon HSPLIT siblings); mon0 TABBED OK. Dry-run then plans `ensure_layout mon1.s0` | Residual after open: `skipWindowStructure = coldEmpty \|\| hasLayoutPh` skipped all window-anchored ensure while skeleton PHs remained. Bind alone when map/PlaceNext left multi-role tab as mon siblings left mon1 ungrouped. Verify focus-only → false ok | **Shipped (code):** `skipWindowStructure = coldEmpty` only; tab/stack ensure while PHs present; mon-level ensure still off while `hasLayoutPh` (skeleton owns mon split). Bind phase then order-phase ensure_layout. Host mid-session re-apply fixed mon1; cold host needs logout tip | L0 `layout-plan-reconcile` residual PH+ungrouped tab; pytest `test_has_layout_ph_ungrouped_tab_emits_ensure_layout`; task [completed](./tasks/completed/forge-layout-residual-tab-ensure.md) |
| R036 | 2026-08-16 | Cold Wayland `forge layout dev`: (1) first tip `hard-failed mon1.s0`; (2) after late-adopt tip **order** `ensure_layout needs flatten of nested CONs` / mon0 nested VSPLIT + mon1 flat; **(3)** provisional tip **crashed host Shell** mid-open (`GetLayoutApply` NoReply / session death) — **repro after “identity-ready” tip too** (job `20260817T023252Z-af18e4`) | **(1–2)** null PlaceNext miss + nested bags + REG-ensure-flatten (provisional FIFO + ensure lift). **(3 crash)** journal: `trackWindow` → sticky PlaceNext **move:true** → `move_to_monitor` SEGV on Wayland chrome null maps; loading “New Tab” re-queue thrash | **Shipped + live PASS 2026-08-16:** no map-time PlaceNext `move_to_monitor` (sticky grace only); late tree + **idle** Meta move; loading titles not identity-ready; place-hint INFO. Nest mon=2 ghosttys PASS (sticky **move=false**). Host cold `layout dev` **ok** · verify match · chrome clear **all-hard** · mon0 tabs + mon1 tabs forest correct | [completed](./tasks/completed/forge-layout-cold-host-verify.md) · L0 place-hint + open-app-policy |
| R037 | 2026-08-17 | **Green** `forge layout dev`: profile `share: [0.687, 0.313]` planned `ensure_sizes` and size ran, but mon stayed 50/50 (`percent=0`). Direct `run-steps` size worked | ApplyLayout post-size `_unwrapMonDirectSingleChildSplits` → `_unwrapSingleChildSplit` **always** called `resetSiblingPercent(mon)`, even on no-op unwrap — wiped mon-level userSized shares | **Shipped:** unwrap only promotes 1-child H/V; copy wrapper `percent`/`userSized` onto promoted child; **never** equalize mon siblings on unwrap. Green layouts already had correct `share` sugar (no profile rewrite). Host green live PASS after install+HUP | L0 `bug-r037-layout-share-post-size-unwrap` + `session-api-layout-cycle` post-size unwrap |
| R038 | 2026-08-18 | **Gray** custom `share` not restored; **green** OK (same tip). Gray sugar `tiles: [{hsplit, share}]` | Bare-array sole `{hsplit\|vsplit}` desugared to mon0 → nested CON `s0` (share on nested); live mon HSPLIT mismatched → `ensure_layout` thrash / hard-fail; green mon-keyed `{mon0:{hsplit, share}}` put share on mon0 | **Shipped:** desugar lifts sole mon-body `[{hsplit\|vsplit, share?}]` to mon-level (JS+Python). Save already avoided emitting that bare form for single-mon. Gray profile rewritten to mon-keyed for loaded tip; nest bare-array dual-ghostty **SHARE_OK** | L0 `test_bare_array_sole_hsplit_share_lifts_to_mon` + normalize fixture `bare-sole-hsplit-share`; nest `_forge-test-share-gray` |
| R039 | 2026-08-19 | `forge layout` leaves manually enlarged splits alone when profile has **no** `share[]` (e.g. host `dev`); structure OK, sizes stick | `sizeActions` only emitted `ensure_sizes` when profile carried `share`; bare hsplit/vsplit had no size step → userSized percents survived apply | **Shipped:** missing share ⇒ equal sibling weights in JS+Python `sizeActions`; AL1 expected dumps regenerated | L0 `layout-plan-reconcile` R039 + `test_plan_equal_sizes_when_share_missing` |
| R040 | 2026-08-19 | `forge install` / job-backed CLI output monochrome in every terminal | Node CN13 `workerShouldForceColor` checked Python-style `.isatty()`; Node streams expose `.isTTY` boolean → force color never set → workers wrote plain logs | **Shipped:** `streamIsTTY` accepts `.isTTY` or `.isatty()`; `runJob`/`spawnWorker` pass attach stream for color detect | L0 `job-runner` `streamIsTTY` / Node `isTTY` force |
| R041 | 2026-08-19 | Wayland `forge install` changes visible tab (e.g. YouTube → Voice); flicker on enable | Install disable→enable reloads extension in-session; session-layout flush only ran on X11 HUP, so Wayland restore used **stale** on-disk open leaves | **Shipped:** `forge_flush_session_layout` before disable; `flushSessionLayout` uses `force:true` | L0 `test_install_safe_replace` flush-before-disable; host: install must not switch tab |
| R003 | 2026-08-08 | left dock open → right mon | dock mon from focus/`get_current_monitor`; weak hook | D007 pointer mon + activate_full | `open-app-policy` focus mon0 + dock mon1; pointer geometry |
| R004 | 2026-08-08 | dock miss → focus mon; mon-root covers left tab | appId mismatch dropped pending; empty LFT(m) → mon-root 3rd HSPLIT child | single-pending match; `_lastTileOnMonitor` end-of-tree | `lft-mru` single unexpired mismatch; `open-app-policy` dock empty mon LFT |

## Agent live E2E (most important)

Unit tests are necessary but **not sufficient** for dual-mon layout. Agents
must run the **partial reload matrix** on X11 (never close agent Ghostty):

| Pre-state | Command |
| --- | --- |
| Ghosttys only | `forge layout dev` |
| Left chrome + ghostty | `forge layout dev` |
| Right ghostty (mon0 chrome gone) | `forge layout dev` |
| Left ghostty + nautilus | `forge layout t1` |

Full procedure + pass criteria: [HANDOFF.md](./HANDOFF.md) § Agent live E2E.
Guards open-leaf steal (R005/R006), mon claim (R001 class), dual ghostty reuse.

## Rules

1. **New live regression** → file row + failing **unit** test when pure, **and** a
   `LIVE_CASES` entry in `scripts/forge/live_matrix.py` tagged with that **R0xx**.  
2. **Do not** only “note in HANDOFF” without a test.  
3. Prefer pure helpers (`lft-mru.js`, `layout_apply.py`) so tests stay fast.  
4. Wayland host Shell cannot HUP — reload extension via **`forge test nested restart`** (AT-W1); dual-mon host CT may still need one logout if host never loaded tip. CLI path is live immediately.  
5. Layout sign-off: **`forge test live plan/run`** with behaviors for the change —
   not the full catalog every time. Full auto only for release / large layout refactors.  
6. Select: `forge test live plan --tags R0xx` or `--from-work open-leaf|cold|…`.

## Related

- OP1 design: [docs/DESIGN.md](../docs/DESIGN.md) open-app placement  
- Decisions: D007, D014–D019, D023–D026 in [docs/DECISIONS.md](../docs/DECISIONS.md)
- Job → API: [docs/dev/contracts.md](../docs/dev/contracts.md)  
- Task history: [completed/forge-dock-sticky-mon.md](./tasks/completed/forge-dock-sticky-mon.md)  
