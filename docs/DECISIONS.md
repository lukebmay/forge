# Design decisions

**How to use:** scan by Topic / Imp. Update in place when a decision changes;
set Status=`superseded` and add a new row (do not rewrite history silently).

| ID | Date | Topic | Imp | Status | Decision | Why |
| --- | --- | --- | --- | --- | --- | --- |
| D001 | 2026-08-06 | theming | P0 | active | Bundled CSS base + user overrides; never full-file patchCss clobber | Upgrades wiped personal colors; user file is deltas not a fork |
| D002 | 2026-08-07 | meta-probe | P1 | active | Intervalic agreement only; soft disagreements recorded, never reset settle timer | Title/focus noise must not block layout settle; keep soft timelines for later chrome/focus work |
| D003 | 2026-08-07 | meta-probe | P1 | superseded | Settle = hard-stable duration; cal@50ms bootstrap 10s; 1 cal+N full/op; write once at end | Superseded by D004 (per-app write + thrash campaign) |
| D004 | 2026-08-07 | meta-probe | P0 | active | Core apps 5×; sticky trials; per-app write; sleep inhibit; thrash delay sweeps | Sleep/crash lost matrices; open_warm piled windows; need thrash-free inter-op D for engine rewrite |
| D005 | 2026-08-07 | meta-probe | P0 | active | Meta baseline: multi-op thrash-free at D=0 (Forge off) on black | Core apps green; product thrash is Forge-induced, not Meta floor |
| D006 | 2026-08-07 | layout | P0 | active | Thrash/fail-open → float client + placeholder TILE leaf; never forest reassert | One bad app must not unsettle forest; close PH = drop slot + one reflow |
| D007 | 2026-08-08 | open-app | P1 | active | Dock sticky mon = pointer geometry; hook activate/open_new_window/activate_full; never rehome dock by focus | get_current_monitor/focus stole left-dock opens to right mon |
| D008 | 2026-08-08 | layout | P0 | active | Cold layout: skeleton (slot-tagged PHs) before bind; no Mode B mid-batch. Still the **materialize-forest** rule under D040 | Residual Mode B second pass is construction-order patch, not product fix |
| D009 | 2026-08-08 | layout | P0 | active | CT1: ensure_skeleton+bind ops; thrash park suppressed on cold/just_opened. One command; Mode B is not cold success | One CLI invocation; postOpenRetry opt-in only |
| D010 | 2026-08-08 | layout | P1 | superseded | Apply chrome from apply start through focus/soft | **Superseded by D043** — overlay dies at all-hard, not after soft |
| D011 | 2026-08-08 | layout | P0 | superseded | Belt re-focus + preserve lastTabFocus on ensure | Superseded by D014 (belt no structure; keep preserve as generic) |
| D012 | 2026-08-08 | layout | P0 | superseded | Final focus after settle + reassert | Superseded by D015 (one post-settle focus; reassert opt-in) |
| D013 | 2026-08-08 | open-app | P0 | active | Empty LFT(m) → last tile on mon; single dock pending wins appId drift | mon-root 3rd HSPLIT covered tabs; dock miss fell to focus mon |
| D014 | 2026-08-08 | layout | P0 | superseded | Post-open belt = pin-role wrong-mon moves only; no ensure_layout/order | **Superseded by D042** — open into slot; belt is a crutch to **delete** (SM6), not a product contract. Do not add belt features |
| D015 | 2026-08-08 | layout | P0 | superseded | One post-settle focus; no reassert | Superseded by D017 — quiet alone does not own late chrome steal |
| D016 | 2026-08-08 | layout | P0 | active | `_layoutOp` still preserves valid lastTabFocus on TABBED/STACKED re-affirm | Mid-session ensure_layout still anchors first id; generic safety not desk-specific |
| D017 | 2026-08-08 | layout | P0 | superseded | Final focus + verify-once only | Superseded by D018 — verify alone lost to multi-second chrome activate |
| D018 | 2026-08-08 | layout | P0 | active | Pin tab open leaf on focus; restore on meta-focus steal; explicit reveal of another child adopts the pin (R026); tab-active=lastTabFocus. Apply path does **not** poll GetTree (D037) | Cold: Chrome over Grok; tab chrome follows keyboard not open leaf; tab click during pin was treated as steal |
| D019 | 2026-08-08 | layout | P0 | active | Hard vs soft **split** + soft heuristics file + pin-on-steal remain. **Execution** (one-shot TILE-anywhere wait → continue; focus-shaped `ok`) **superseded by D040/D041** | Fixed ms reassert brittle; Meta has no settle ACK. Timeout-continue hid empty-mon fails |
| D020 | 2026-08-09 | windows | mid | active | `mode: "ignore"` in windows.json: no tree node / decorations / session claim; user config only (no bundled brands) | Float still tracks + processFloats; some apps need true hands-off |
| D021 | 2026-08-09 | CLI | P0 | active | Mutating forge commands run as durable one-shot jobs by default (attach); `--detach` no-wait only; no flag required for TTY survival; single-flight mutators; not a daemon | Closing agent TTY mid-layout aborted apply; session-bound workers inherit DBus/DISPLAY |
| D022 | 2026-08-10 | nest-test | P0 | active | Nest = value-first retest harness (avoid Wayland logout loops); separate logical host data (`FORGE_HOST` + nest config root); no UNIX test user v1; auto stop/cleanup; nest only when extension reload needed; host for no-code smokes; nest default 1 mon — multi-mon only when testing multi-mon behavior | Shared HOME heuristics/config taint parent; dual-mon dummy nest unblocks structure loop only when needed; test-user setup cost > isolation gain until data-root fails |
| D023 | 2026-08-12 | tree | P0 | active | Child list via Node (`appendChild`/`insertBefore`/`removeChild`/`replaceChildren`); no `childNodes`/`parentNode` assigns outside Node | R018 install HSPLIT swap was a hand-rolled splice; catalog Canonical APIs |
| D024 | 2026-08-13 | dnd | P0 | active | Drop no-op iff parent+order+layout already match; CENTER on H/V siblings is a group op via `mergeWindowsIntoGroup` | `_isNoOpDrop` treated CENTER as “already after sibling” so Grok→Chrome did nothing |
| D025 | 2026-08-13 | focus | P0 | active | Live show-in-group via one WM helper (`revealGroupChild`); snapshot may write `lastTabFocus` as data only; reveal reasserts that child to slot (R025); adopt live pin if revealing another child (R026) | Agents re-coded LTF+raise; tab strip / pin / activate drifted; hidden tab stayed FLOAT/old-slot; first tab click after layout snapped back |
| D026 | 2026-08-13 | geom | P0 | active | TILE slot is authority; unsolicited size/max/Meta-fs (no grab, no forge echo) restores to slot. **Idle-only** — not during ApplyEpoch or grab (D039). Zoom later is a presentation flag | VLC/end-of-video fs skipped by `tree.apply`; mid-apply restore fights slot machines |
| D027 | 2026-08-13 | open-app | P0 | active | Empty dest head (pointer mon, else window mon) beats LFT/focus; dock still wins; generic tiled-head opens stay OP1 LFT | Dock-miss + left LFT yanked right-head opens onto left end-of-tree (R021) |
| D028 | 2026-08-13 | dnd | P0 | active | Empty-mon user drop moves the dragged **leaf** only; workspace-change may still migrate a CON | `_rehomeWindowPreservingContainer` walked the VSPLIT when sibling Meta mon matched dest (R022) |
| D029 | 2026-08-13 | dnd | P0 | active | Orientation-mismatch edge drop wraps the target; never reuse a MONITOR that already has siblings | `numWin === 2` MONITOR reuse flattened BOTTOM into a 3-wide HSPLIT (R023) |
| D030 | 2026-08-13 | zoom | P1 | active | Presentation `zoomMode` on WINDOW (`null`/`full`/`horizontal`/`vertical`); `tree.apply` paints `zoomRect`; any chord clears any mode; not Meta max/fs/monocle | IC3 would undo Meta maximize; zoom is a paint flag (I4 slots stay) |
| D031 | 2026-08-13 | keybinds | P1 | active | Install does not rewrite kits unless `--kit=vim\|safe\|i3`; after install warn when live matches no kit; `--kit` loads that kit. CLI: `keybind save\|load` (no --reset/--profile); `vim`/`safe`/`i3` reserved profile names | Kit chords change in tree; dconf is sticky; Super+Enter stayed swap-last after zoom remap |
| D032 | 2026-08-13 | insert | P0 | active | New tiled window / same-axis edge drop **slot-splits the focused (or drop-target) unit** when that unit’s H/V parent already has siblings. Never a 3rd even H/V sibling. A leftover 1-child H/V CON **is** the slot (join it; retarget aspect). Orientation from the **slot rect**, not a stale Meta frame. 2nd on empty mon stays a MONITOR sibling. Drag: reorder in strip/row; CENTER joins; empty-mon leaf-only (R022); peel wrap-in-slot | Operator: 3 Nautilus became even HSPLIT; leftover wrap attached to MONITOR; batch skip. Even 3-way only after resize or `window-reset-sizes`. |
| D033 | 2026-08-13 | keybinds | P2 | active | Empty `launch-app-command` opens GNOME Run a Command (`Main.openRunDialog`); non-empty still spawns the command. Vim/i3 Run is Super+Space. `toggle-maximized` is cleared while Forge is enabled so Super+Return is Forge zoom; host-defaults must not bind it | Operator wanted Super+Enter maximize (GNOME-like) and Super+Space launch; leftover host-defaults Super+Return maximize stole zoom |
| D034 | 2026-08-13 | layout | P0 | active | Map-pin: title while waiting, then class-only leftover assign. Replan class-only claim. **Serialize chrome-family opens.** Late title re-renders like late wm-class. Product apply waits on **Meta signals**, not CLI GetTree poll | Green: parallel Chrome+Grok crashed Chrome; Grok mapped as New Tab so wait missed it |
| D035 | 2026-08-13 | layout | P0 | active | Layout pin/plan use tree windows **plus** a Meta census. `trackWindow` falls back to a live monitor node instead of dropping. `admitUntrackedWindows` before plan and during map-wait. Timeout dumps `seen` + `includeMeta` | R029 class-only pin cannot see X11 windows that never entered the tree; second apply launched again |
| D036 | 2026-08-14 | CLI | P0 | active | Control CLI language is Node. Product policy lives in `lib/shared/` as **gi-free** ESM. Prefs/extension/CLI are facades (GJS vs Node I/O). Migrate by subcommand (CN0–CN6). Python router stays on PATH until CN13. Zsh install stays in `scripts/`. **Do not** port `layout_plan`/`layout_apply` into `cli/` | Dual Python/JS drifted (keybind); Node is the product language; a cli/ planner port would be thrown away by D037 |
| D037 | 2026-08-14 | layout | P0 | active | Layout rearchitecture is in-process `ApplyLayout` (extension owns the run; wait on Meta signals). DBus/job/planner shape is D038. **Executor shape is D040** (slot machines), not the AL7 phase spine. IC4 CLI-waiter fold is **skipped** | CLI GetTree polls are the slowness; language port of the poll loop is not |
| D038 | 2026-08-15 | layout | P0 | active | `ApplyLayout` is async start (not a blocking DBus call) plus `GetLayoutApply` / `CancelLayoutApply` and Progress/Done signals. Host D021 job is observer only. `planReconcile` lives in `lib/shared/layout-plan.js` (GetTree JSON in / actions out). LayoutBatch stays a primitive. R027 chrome remains in-progress; lifetime = apply epoch through all-hard (D043). Executor never `_layoutOp` flatten | GNOME DBus timeout cannot hold the spine; TTY survival stays host-side; planner must be gi-free; REG-ensure-flatten is not the cold path |
| D039 | 2026-08-16 | layout | P0 | active | ApplyEpoch / home authority: while ApplyLayout is live, desired forest is the only writer of mon membership and TILE home. `beginApplyEpoch` / `endApplyEpoch` replace ad-hoc `_layoutApplyLive`. Drop deferred entered-monitor rehomes on enter and leave. User grab and workareas are different epochs. Workareas/monitors-changed during apply → cancel (`displays-changed`). D026 TILE restore is idle-only (not during apply epoch or grab) | R036: entered-monitor rehome after open undid mon place; boolean suppress on WindowManager is a gate, not an API |
| D040 | 2026-08-16 | layout | P0 | active | Slot machines amend D019 execution. A **slot** is a desired-forest leaf (one TILE window **or** one TABBED/STACKED CON). Parallel across independent slots; serial inside a slot. Hard = in-slot (TILE\|grab + desired mon + desired parent CON + ε rect). Timeout retries that slot’s place (N=2 extra; first wait 5s, retry 2s). Late Meta resume only while the epoch is live. Soft + focus after all required slots terminal. One-shot hard continue is dead | TILE-anywhere hard + continue-on-timeout let `layout` report ok with mon1 empty; windows are not independent |
| D041 | 2026-08-16 | layout | P0 | active | `Done.ok` = forest match for every **required** TILE slot. Focus verify is not success. Required `hard-failed` → `ok: false`, `code: hard-failed`, slot list. Peers always finish (execution continues). FLOAT / `ignore` / non-tile are not required hard targets. Frequent hard-fails mean the hard contract is wrong — do not flip default `ok` true. No standing best-effort success | False-ok (R035/R036) hid structure bugs; hard-fail should be rare once hard means in-slot |
| D042 | 2026-08-16 | layout | P0 | active | Apply open binds **into the slot** (skeleton PH or slot id). PlaceNext dest is never mon-root-only on the product apply path. Belt + `beltStructure` are **not** product architecture (delete in SM6). **Supersedes D014** | Four-pass place fought Meta and lied about success |
| D043 | 2026-08-16 | chrome | P0 | active | TABBED/STACKED: option A — singular CON decoration (one strip + pane). Partial draw as members hard-complete is OK. Open-leaf = restack + `lastTabFocus`, not N chromes. Option B (per-app chrome swap) rejected. Apply overlay lifetime = epoch through all-hard (or failed). Tab D0 locked: existing CON strip **is** option A; no extra apply-time group chrome. FCC C3 (H/V split chrome) stays later | N full chromes thrash; overlay must not outlive honest hard; D0 must not swallow apply rewrite or invent a second strip |
| D044 | 2026-08-16 | tabs | P0 | active | TABBED/STACKED CON is **mon-local**. Cross-mon groups are not product. Mixed-mon members rehome to the CON’s MONITOR ancestor (keep the group; do not auto-peel). Join across mons = move-then-join onto dest. Keyboard mon-move of one tab peels that leaf. No profile span sugar. H1 majority-align + R016 mon-loss collect stay survival-only | Operator did not know groups could straddle; one strip+pane cannot paint two heads; slot machines assume one mon per slot |
| D045 | 2026-08-17 | CLI | P1 | active | User `forge` is product-only (layout/tree/ops/settings/install). Nest + live matrix live on **`forge-test`** (`./scripts/forge/forge-test`). Normal `./install` does not put `forge-test` on PATH; `--with-test-cli` is opt-in. `forge test` / `forge nested` hard-break (exit 2) | User CLI ≠ dev toolkit; nested-under-`forge test` still shipped harnesses to ordinary install |
